import React from 'react';
import ReactDOM from 'react-dom/client';
import Input from './comp/Input'; // Assuming Input component is in the same directory

const root = ReactDOM.createRoot(document.getElementById('root'))
root.render(
  <Input/>
)
